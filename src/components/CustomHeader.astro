---
import LanguageSelect from '@astrojs/starlight/components/LanguageSelect.astro';
import Search from '@astrojs/starlight/components/Search.astro';
import SiteTitle from './SiteTitle.astro';
import SocialIcons from '@astrojs/starlight/components/SocialIcons.astro';
import ThemeSelect from './ThemeSelect.astro';

const curLocale = Astro.locals.starlightRoute.locale;
const slug = Astro.locals.starlightRoute.entry.slug;

const showBanner = (Astro.locals.starlightRoute.entry.data as any).showBanner !== false;
const bannerMessage = (Astro.locals.t as (key: string) => string)('banner.message');
const hasBanner = showBanner && bannerMessage && bannerMessage !== 'banner.message';

type Link = {
  title: string;
  value: string;
  transfer: boolean;
};

// Note that these links are duplicated in SiteTitle.astro and CustomHeader.astro
function formatStarCount(n: number): string {
  if (n >= 1000) return (n / 1000).toFixed(1).replace(/\.0$/, '') + 'k';
  return String(n);
}

let starCount = '';
try {
  const headers: Record<string, string> = { 'Accept': 'application/vnd.github.v3+json' };
  const token = import.meta.env.GITHUB_TOKEN ?? process.env.GITHUB_TOKEN;
  if (token) headers['Authorization'] = `Bearer ${token}`;
  const res = await fetch('https://api.github.com/repos/skiptools/skip', { headers });
  if (res.ok) {
    const data = await res.json();
    if (data?.stargazers_count) {
      starCount = formatStarCount(data.stargazers_count);
    }
  }
} catch {}

const links: Link[] = [
  {
    title: Astro.locals.t('header.link.docs'),
    value: '/docs/',
    transfer: true,
  },
  {
    title: Astro.locals.t('header.link.blog'),
    value: '/blog/',
    transfer: false,
  },
  {
    title: Astro.locals.t('header.link.sponsor'),
    value: '/sponsor/',
    transfer: true,
  },
];
---
{hasBanner && (
  <div class="site-banner" role="status" data-banner hidden>
    <span class="banner-text" set:html={bannerMessage} />
    <button class="banner-close" type="button" aria-label="Dismiss banner">
      <svg width="10" height="10" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path d="M1.5 1.5L12.5 12.5M12.5 1.5L1.5 12.5" stroke="currentColor" stroke-width="1.75" stroke-linecap="round"/>
      </svg>
    </button>
  </div>
)}
<script is:inline>
  (function() {
    var banner = document.querySelector('[data-banner]');
    if (!banner) return;
    var key = 'banner-dismissed-at';
    var ts = localStorage.getItem(key);
    if (ts && Date.now() - Number(ts) < 30 * 86400000) {
      banner.remove();
      return;
    }
    localStorage.removeItem(key);
    document.body.prepend(banner);
    banner.removeAttribute('hidden');
    banner.querySelector('.banner-close').addEventListener('click', function() {
      localStorage.setItem(key, String(Date.now()));
      banner.remove();
    });
  })();
</script>
<div class="header sl-flex">
  <div class="full-width sl-flex">
    <SiteTitle />
  </div>
  <div class="sl-flex">
    <Search />
  </div>
  <div class="sl-hidden md:sl-flex right-group">
    <div class="desktop">
      {
        links.map((link) => (
          <a href={link.transfer && curLocale ? `/${curLocale}${link.value}` : link.value}>
            {link.title}
          </a>
        ))
      }
    </div>
    <a
      class="gh-stars"
      href="https://github.com/skiptools/skip"
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Star us on GitHub"
      data-gh-stars
    >
      <svg fill="none" viewBox="0 0 24 24" width="20" height="20" aria-hidden="true">
        <path fill="currentColor" d="M12 1.60205C5.9225 1.60205 1 6.41965 1 12.3676C1 17.1314 4.14875 21.155 8.52125 22.5815C9.07125 22.6757 9.2775 22.3527 9.2775 22.0701C9.2775 21.8144 9.26375 20.9666 9.26375 20.065C6.5 20.5629 5.785 19.4056 5.565 18.8001C5.44125 18.4906 4.905 17.5351 4.4375 17.2794C4.0525 17.0776 3.5025 16.5797 4.42375 16.5662C5.29 16.5528 5.90875 17.3467 6.115 17.6697C7.105 19.298 8.68625 18.8404 9.31875 18.5578C9.415 17.8581 9.70375 17.3871 10.02 17.1179C7.5725 16.8488 5.015 15.9203 5.015 11.8024C5.015 10.6317 5.44125 9.66278 6.1425 8.90919C6.0325 8.64005 5.6475 7.53658 6.2525 6.05631C6.2525 6.05631 7.17375 5.77372 9.2775 7.15978C10.1575 6.91756 11.0925 6.79645 12.0275 6.79645C12.9625 6.79645 13.8975 6.91756 14.7775 7.15978C16.8813 5.76026 17.8025 6.05631 17.8025 6.05631C18.4075 7.53658 18.0225 8.64005 17.9125 8.90919C18.6138 9.66278 19.04 10.6182 19.04 11.8024C19.04 15.9337 16.4688 16.8488 14.0213 17.1179C14.42 17.4544 14.7638 18.1003 14.7638 19.1096C14.7638 20.5495 14.75 21.7068 14.75 22.0701C14.75 22.3527 14.9563 22.6891 15.5063 22.5815C17.69 21.86 19.5875 20.4865 20.9318 18.6542C22.2761 16.822 22.9994 14.6233 23 12.3676C23 6.41965 18.0775 1.60205 12 1.60205Z"/>
      </svg>
      <span class="gh-stars-count">{starCount}</span>
    </a>
    <div class="sl-flex social-icons">
      <SocialIcons />
    </div>
    <LanguageSelect />
    <ThemeSelect />
  </div>
</div>

<style>
  /* ── Site-wide announcement banner ── */
  /* JS moves the banner to <body> so it's independent of the header. */
  .site-banner {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: center;
    height: var(--sl-banner-height);
    padding: 0 2.5rem 0 1rem;
    background: linear-gradient(90deg, hsl(38, 65%, 20%), hsl(28, 60%, 23%));
    border-bottom: 1px solid hsl(38, 50%, 30%);
    font-size: var(--sl-text-sm);
    line-height: 1.4;
    text-align: center;
    color: hsl(40, 55%, 88%);
  }
  .banner-text {
    display: inline;
  }
  .banner-close {
    position: absolute;
    inset-inline-end: 0.75rem;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    align-items: center;
    justify-content: center;
    width: 1rem;
    height: 1rem;
    background: hsla(0, 0%, 100%, 0.1);
    border: 1px solid hsla(0, 0%, 100%, 0.15);
    border-radius: 0.25rem;
    color: hsl(40, 40%, 75%);
    cursor: pointer;
    padding: 0;
    transition: background 0.15s, color 0.15s, border-color 0.15s;
  }
  .banner-close:hover {
    background: hsla(0, 0%, 100%, 0.2);
    border-color: hsla(0, 0%, 100%, 0.3);
    color: hsl(40, 55%, 95%);
  }
  :root[data-theme='light'] .banner-close {
    background: hsla(30, 50%, 30%, 0.08);
    border-color: hsla(30, 50%, 30%, 0.2);
    color: hsl(30, 40%, 40%);
  }
  :root[data-theme='light'] .banner-close:hover {
    background: hsla(30, 50%, 30%, 0.15);
    border-color: hsla(30, 50%, 30%, 0.35);
    color: hsl(30, 45%, 25%);
  }
  .banner-text :global(a) {
    color: hsl(40, 100%, 72%);
    font-weight: 700;
    text-decoration: underline;
    text-underline-offset: 2px;
  }
  .banner-text :global(a:hover) {
    color: hsl(40, 100%, 85%);
  }
  :root[data-theme='light'] .site-banner {
    background: linear-gradient(90deg, hsl(40, 80%, 92%), hsl(35, 75%, 88%));
    border-bottom-color: hsl(38, 55%, 80%);
    color: hsl(30, 45%, 22%);
  }
  :root[data-theme='light'] .banner-text :global(a) {
    color: hsl(25, 85%, 38%);
  }
  :root[data-theme='light'] .banner-text :global(a:hover) {
    color: hsl(25, 90%, 30%);
  }

  .header {
    gap: var(--sl-nav-gap);
    justify-content: space-between;
    align-items: center;
    height: 100%;
  }
  .full-width {
    width: 100%;
  }
  .right-group,
  .social-icons {
    gap: 1rem;
    align-items: center;
  }
  .gh-stars {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    height: 2.25rem;
    padding: 0 1rem 0 0.75rem;
    font-size: var(--sl-text-sm);
    font-weight: 500;
    line-height: 1;
    white-space: nowrap;
    border-radius: 1.8rem;
    /* border: 1px solid var(--sl-color-gray-5); */
    background: var(--sl-color-gray-6);
    text-decoration: none;
    color: var(--sl-color-gray-2);
    transition: background 0.15s, border-color 0.15s;
  }
  .gh-stars:hover {
    background: var(--sl-color-gray-5);
    border-color: var(--sl-color-gray-4);
    color: var(--sl-color-gray-2);
  }
  .gh-stars:active {
    transform: scale(0.98);
  }
  .gh-stars svg {
    flex-shrink: 0;
    color: var(--sl-color-gray-3);
  }
  .gh-stars-count {
    line-height: 1;
  }
  .gh-stars-count:empty::after {
    content: '\00B7\00B7\00B7';
    opacity: 0.4;
  }
  :root[data-theme='light'] .gh-stars {
    color: var(--sl-color-gray-2);
    background: var(--sl-color-gray-6);
    border-color: var(--sl-color-gray-5);
  }
  :root[data-theme='light'] .gh-stars:hover {
    background: var(--sl-color-gray-5);
    color: var(--sl-color-gray-2);
  }
  :root[data-theme='light'] .gh-stars svg {
    color: var(--sl-color-gray-3);
  }
  .desktop,
  .release-link::after,
  .social-icons::after {
    content: '';
    height: 2rem;
    border-inline-end: 1px solid var(--sl-color-gray-5);
  }

  @media (min-width: 50rem) {
    :global(:root[data-has-sidebar]) {
      --__sidebar-pad: calc(2 * var(--sl-nav-pad-x));
    }
    :global(:root:not([data-has-toc])) {
      --__toc-width: 0rem;
    }
    .header {
      --__sidebar-width: max(0rem, var(--sl-content-inline-start, 0rem) - var(--sl-nav-pad-x));
      --__main-column-fr: calc(
        (
            100% + var(--__sidebar-pad, 0rem) - var(--__toc-width, var(--sl-sidebar-width)) -
              (2 * var(--__toc-width, var(--sl-nav-pad-x))) - var(--sl-content-inline-start, 0rem) -
              var(--sl-content-width)
          ) / 2
      );
      display: grid;
      grid-template-columns:
        /* 1 (site title): runs up until the main content column's left edge or the width of the title, whichever is the largest  */
        minmax(
          calc(var(--__sidebar-pad, 0rem)),
          auto
        )
        /* 2 (search box): all free space that is available. */
        1fr
        /* 3 (right items): use the space that these need. */
        auto;
      align-content: center;
    }
  }
  a:hover {
    color: var(--sl-color-gray-2);
  }
  :root[data-theme='light'] {
    .header a:hover {
      color: var(--sl-color-gray-3);
    }
  }
</style>
<style is:global>
  .header {
    border: none !important;
  }
body[data-mobile-menu-expanded] header {
  background-color: var(--sl-color-bg);
}

/* ── Site-wide banner offset ──
   JS moves the banner to <body>, so it's a direct child of body.
   When it exists, push the header and all content down by --sl-banner-height. */
:root {
  --sl-banner-height: 0rem;
}
body:has(> .site-banner) {
  --sl-banner-height: 2rem;
}
body:has(> .site-banner) header {
  top: var(--sl-banner-height) !important;
}
body:has(> .site-banner) .sidebar-pane {
  top: calc(var(--sl-nav-height) + var(--sl-banner-height)) !important;
}
body:has(> .site-banner) .main-frame {
  padding-top: calc(var(--sl-nav-height) + var(--sl-mobile-toc-height) + var(--sl-banner-height)) !important;
}
body:has(> .site-banner) .right-sidebar {
  padding-top: calc(var(--sl-nav-height) + var(--sl-banner-height)) !important;
}
body:has(> .site-banner) mobile-starlight-toc nav {
  top: calc(var(--sl-nav-height) + var(--sl-banner-height) - 1px) !important;
}
body:has(> .site-banner) {
  scroll-padding-top: calc(1.5rem + var(--sl-nav-height) + var(--sl-mobile-toc-height) + var(--sl-banner-height)) !important;
}
@media (min-width: 72em) {
  body:has(> .site-banner) {
    scroll-padding-top: calc(1.5rem + var(--sl-nav-height) + var(--sl-banner-height)) !important;
  }
}
</style>

{
slug === '' || slug === curLocale ?
<style>
  .header {
    background-color: transparent;
      -webkit-backdrop-filter: blur(16px);
    backdrop-filter: blur(16px);
    padding-inline-end: calc(var(--sl-nav-gap) + var(--sl-nav-pad-x));
  }

  @media (min-width: 50rem) {
    header {
      background-color: transparent !important;
    }
  }
  @media (min-width: 50rem) {
    .header {
      padding-inline-end: var(--sl-nav-pad-y);
    }
  }
</style>
: ''
}

<style>
  .desktop {
    display: none;
    align-items: center;
    gap: 1rem;
    padding-inline-end: 1rem;
  }
  @media (min-width: 72rem) {
    .desktop {
      display: flex;
    }
    .mobile {
      display: none;
    }
  }

  a {
    color: var(--sl-color-white);
    font-weight: 600;
    text-decoration: none;
  }

  /* From https://github.com/withastro/starlight/blob/main/packages/starlight/components/Select.astro */
  label {
    --sl-label-icon-size: 0.875rem;
    --sl-caret-size: 1.25rem;
    position: relative;
    display: flex;
    align-items: center;
    gap: 0.25rem;
    color: var(--sl-color-gray-1);
  }

  label:hover {
    color: var(--sl-color-gray-2);
  }

  .icon {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    pointer-events: none;
  }

  .label-icon {
    font-size: var(--sl-label-icon-size);
    inset-inline-start: 0;
  }

  .caret {
    font-size: var(--sl-caret-size);
    inset-inline-end: 0;
  }

  select {
    border: 0;
    padding-block: 0.625rem;
    padding-inline: calc(var(--sl-label-icon-size) + 0.25rem) calc(var(--sl-caret-size) + 0.25rem);
    width: var(--sl-select-width);
    background-color: transparent;
    text-overflow: ellipsis;
    color: inherit;
    cursor: pointer;
    appearance: none;
  }

  option {
    background-color: var(--sl-color-bg-nav);
    color: var(--sl-color-gray-1);
  }

  @media (min-width: 50rem) {
    select {
      font-size: var(--sl-text-sm);
    }
  }
</style>
